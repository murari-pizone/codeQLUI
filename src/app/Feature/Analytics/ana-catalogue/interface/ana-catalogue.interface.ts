export interface AnaCatalogueTab {
    name: string;
    label: string;
}

export interface AnaRows{
    name: string, 
    netRevenue: string, 
    change: string, 
    noOfItems: number, 
    avgPrice: string
}