import { ShowItemModalTabPipe } from './show-item-modal-tab.pipe';

describe('ShowItemModalTabPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowItemModalTabPipe();
    expect(pipe).toBeTruthy();
  });
});
